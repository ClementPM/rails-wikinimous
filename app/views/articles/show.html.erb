
<h2><%= @article.title %></h2>
<p><%= @article.content %></p>

<%= link_to article_path(@article), method: :delete, data: {confirm: "Are you sure?"}, class: 'btn btn-danger' do %> 
<i class="far fa-trash-alt"></i> Delete

<% end %>

 <%= link_to 'See all articles', articles_path, class: 'btn btn-secondary' %>
